import React from "react";
import { Tilt } from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className='xs:w-[250px] w-full'>
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className='w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card'
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className='bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col'
      >
        <img
          src={icon}
          alt='web-development'
          className='w-16 h-16 object-contain'
        />

        <h3 className='text-white text-[20px] font-bold text-center'>
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className='mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]'
      >
      I'm a third-year Software Engineering student with a deep passion for building scalable, real-world applications. My technical foundation is rooted in core Java and Data Structures & Algorithms, and I am currently expanding my backend architecture expertise by mastering Spring Boot and containerization with Docker. 
      <br /><br />
      Beyond the backend, I have a working knowledge of web technologies like HTML and JavaScript, and hands-on experience developing cross-platform mobile solutions using Flutter. Whether I am collaborating with a team of developers at a national-level hackathon or tackling a rigorous 30-day technical coding sprint to sharpen my problem-solving skills, I thrive in fast-paced, challenging environments. 
      <br /><br />
      I am an enthusiastic learner actively seeking an SDE internship for 2026, eager to contribute to high-impact products and continuously grow my technical toolkit alongside industry experts.
      </motion.p>

      <div className='mt-20 flex flex-wrap gap-10'>
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
